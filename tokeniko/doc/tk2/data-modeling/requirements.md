# data-modeling — REQUIREMENTS

*What the rows ARE. Distillation session 2026-08-23 (author's ruling: mostly tk1's logic, reorganized
+ the tk2 adaptations). One line each.*

1. **The tk1 tiers carry over as logic** — definitions · axioms · theorems · memory (timeseries) · stakeholders · behavior rules; the biography is sacred, `original` verbatim, one item one home.
2. **The two-matrix base replaces the Jurassic** — R (stated, signed, provenance per cell) and D (topical, unsigned) over one POS-split key space (dictionary reqs).
3. **The new organs get their rows**: heart levels/mood/temperament + the per-uid target table + the emotional log · staked FORECASTS (confidence + future spacetime, memory's mirror) · epoch-stamped derived points · micro-nn instances (config + weights) · channel registers.
4. **Three write-classes, declared per collection** — kb (rw) · parameters (r) · hardwired-logic tables (r); the everything-is-rows seam is a schema property.
5. **Provenance is a first-class field everywhere** — every derived thing names its parents; summaries retreat with them.
6. **Schemas live with the entities** (pydantic models remain the single source of shape); a dimension change is a migration, never a cast.
