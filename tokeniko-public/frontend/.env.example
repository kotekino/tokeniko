# Backend API base — BAKED AT BUILD TIME by Vite (set before `npm run build`).
# Dev: the Vite proxy forwards /api → :4000, so the default is fine.
# Prod: the backend App Service URL, e.g. https://tokeniko-api.azurewebsites.net/api
VITE_API_URL=http://localhost:4000/api

VITE_SITE_NAME=tokeniko

# Coming-soon gate. Unset/0 = full site (default). 1 = the coming-soon page
# becomes the ONLY page (rebuild required — Vite inlines this at build time).
# VITE_COMING_SOON=0

# tokeniko's Discord playground — the invite CODE only (the part after discord.gg/), so it can be
# rotated without a code change. MUST carry the VITE_ prefix: Vite exposes nothing else to the
# client, and it is INLINED AT BUILD TIME — rotating the code means rebuild + redeploy, not a
# restart. Unset = the Discord CTAs are hidden (no stale link is ever shipped).
VITE_DISCORD_INVITE_CODE=