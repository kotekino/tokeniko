/.DS_Store
/data
/dump
/atlas
/docs/~$notes.xlsx
__pycache__/
*.pyc
# Claude Code local state stays private — but the CREW's working record (the 1st Officier's
# standing contract + the QM's briefs) is public, as the wiki's Collaboration page documents.
.claude/*
!.claude/agents/
!.claude/briefs/

# secrets — never commit (env files hold tokens / DB URIs)
.env
ci/.DS_Store

# the body (body/tools/): the LaunchAgents' log destination, and the machine-local address of
# the mini. Both are per-machine facts, not code.
/tokeniko-tk1/logs/
/body/tools/body.conf
ci/.DS_Store
ci/1.0/.DS_Store
ci/1.0/assets/.DS_Store
ci/.DS_Store
.aider*
