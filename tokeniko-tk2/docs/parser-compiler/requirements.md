# parser/compiler — REQUIREMENTS

*What the one station must be. One line each. Evidence in the dated `_notes.md` beside this.*

1. **One station, one format** — tkzip is the only thing that crosses the boundary; internal scratch is private, never stored, never a contract; tk1's AST/LLC die as interfaces.
2. **The skeleton provider is a demoted, swappable proposer** — stanza kept for tokens/POS/deps (best-in-class, local); an implementation detail behind the lib boundary; its stumble is rag-in's case. **[AMENDED 2026-09-15 — stanza is the provider because it TARGETS A STANDARD. `spacy-stanza` may be the API surface; spaCy's own models are neither a second opinion nor a fallback, because «stanza tries to be as close to UD2 as possible while spaCy is more creative» (the Captain) — two readers where only one is trying to be correct is one reader plus noise. **The second reader is UD2 itself**: 37 relations and 17 POS tags with published examples, checked relation by relation. «Close enough» is a measurement E3 owes, not an assumption it carries. Record: `202609151148_the-ud-gate-and-the-stations-edges.md`. **MEASURED 2026-09-15: stanza agrees with UD's own published parse on 9 of the 12 sentences UD annotates in full; the three differences are named in `202609151259_the-ud-gate-opens.md` and two of them are stanza being MORE specific than the page. «Close enough» is now a number. **AND A SECOND ONE, 2026-09-16: over the eighteen relations transcribed when the gate reached all 37, stanza produces 15 of the 18 edges UD publishes. The three it does not — `goeswith`, `orphan`, `reparandum` — are ONE FAMILY: a typo, a gapping, a self-correction. The station will never meet them labelled from this provider, which is worth knowing before anything is built against them.**]** **[SHARPENED 2026-09-22 — DEMOTED IS NOT IGNORED, AND A CREDIBLE TAG IS EVIDENCE.** `ClosedClasses.select` returned a single-row form without ever consulting the POS, and **221 of the 331 forms have exactly one row** — so for two thirds of the table the match was made on the SPELLING ALONE. «Every human BEING is an animal» compiled to «An animal is.»: the noun matched the copula's participle, was read as structure, and took the whole subject with it. At least seventeen of those forms are ordinary English words — `back` · `can` · `will` · `need` · `one` · `past` — and «the BACK of the house», «a CAN of soup», «the WILL of the people» each lost a noun. **This table holds no content words**, and `UD_POS_TO_WORD_CLASS` having no entry for `NOUN` · `PROPN` · `ADJ` is evidence, not an omission. `VERB` is deliberately excluded: English HAS a noun «being» so that tag is right, and has NO verb «through» so that one is a parse error — which is the disagreement `select` is right to look past. Record `202609221200_a-content-word-is-not-a-function-word.md`.]**
3. **The journey shields the misparse** *(shield (1), disagreement between skeletons, is DEAD — see req 2's amendment; shields (2) and (3) below are the real protection)* — the evaluator's incoherence on fresh input flags parse-suspicion (mandatory second reader); below the floor the BRAIN asks by rendering the reading back; `original` stays verbatim so even a landed misreading is retreatable.
4. **Every zip carries a CONFIDENCE scalar** — «this zip IS what I received»: a calibrated aggregate of coverage · repairs-taken · self-round-trip; tk1 never had one. **[MEASURED AND DEFERRED, 2026-09-20 — and deferred on evidence rather than on effort.** `tools/confidence_bench.py` puts the station's own signals against both truth columns it has. **Against the round trip no band is DECISIVE**: a sentence with perfect coverage, nothing unplaced, nothing abstained and nothing defaulted still fails it 14 times in 52, and one with a word unplaced still survives 9 times in 21 — the ranges overlap completely, which is the placement floor's shape and the placement floor's answer. **Against the drill gate the signals point the RIGHT way** — `defaulted` 0.317 → 0.800 and `unsaid` 0.159 → 0.800 from agreed to DISAGREED — **but the negative population is five cases**, and nothing is calibrated on five points. *And the round trip is not yet the truth column it will be: it counts a zip as moved when the DECOMPILER cannot say it, not only when the parse was ambiguous, so 17 of its 24 failures are the decompiler's own.* **So the bookkeeping is free and is kept; the scalar stays EMPTY until it can mean something** — writing a number from uncalibrated weights would claim a calibration that does not exist, which is the objection req 58 already makes to writing `1.0` for an utterance that never happened. **The dependency runs the other way from how the plan had it**: this waits on the decompiler's own remaining failures, not the reverse. The Captain: «agree with your lean».]**
5. **Calibration is the seventh micro-nn site** — corrections train it down, smooth dialogue up; it steers choice (proceed / rag-in / ask), never truth.
6. **The self-round-trip is escalation-only** — cheap signals are free bookkeeping; the second read fires only on ambiguity, plus lazy sleep spot-calibration; clean input costs ONE parse.
7. **The station is pure — context is an argument, never state** — anaphora, ellipsis, fragments resolve against caller-provided recent zips; zero hidden memory. **[BUILT 2026-09-16 (E3 task 2b.1), after being written at conception and unbuilt through the whole epic — «nothing has needed it yet» until the person axis did. `compile(skeleton, context)` carries the speech act's SPEAKER and ADDRESSEE, and `recent` is declared for the anaphora that is still not built, so the argument does not change shape the day it is. The context is READ and never stored: two calls with different contexts cannot influence each other, and **without a context nothing changes at all**. Record: `202609161521_context-as-an-argument.md`.]**
8. **Half-understood is legal, wrongly-understood is the sin** — beyond-format input yields a partial zip honestly marked (open variables + confidence), never a silently-complete nearest fit.
9. **Bidirectional — the station COMPILES and DECOMPILES** — the decompiler (zip → a sentence that compiles back to that zip) lives in the same lib; compile and decompile symmetric; the round-trip is internal machinery. **[RENAMED 2026-09-19, the Captain: «I would call it DECOMPILER, mimicking the compiler/decompiler of computer languages».** The word was «scaffold», and it was wrong twice over: it named the output rather than the operation, and tk1 already uses `TKScaffoldDoc` for curated VOICE TEMPLATES, an unrelated thing wearing the same word (third standing law). The analogy is exact and it carries the acceptance test with it: decompiled source is never the original source, it is *a* source that compiles to the same object — so the test is RECOMPILE AND COMPARE THE ZIPS, which is what the round-trip already is. **And the decompiler is faithful to the ZIP, not to the original sentence**: where the parse normalised (a passive folded to active, a pronoun resolved, a word left unplaced) the decompilation shows what the zip holds, and that gap IS the misparse signal req 3 asks the brain to speak back.]** **[SECOND AND THIRD SLICES, 2026-09-20, on the Captain's «everything that emerged should be fixed … until the input sentence is 100% equivalent to the output one; the decompiler is another CORE feature, so it must be perfect from the beginning — at least it should be the reverse operation of what the compiler does».** That instruction needed an instrument the drill comparison cannot be: **`tools/roundtrip.py --fixpoint`, the station against itself** — sentence → zip → sentence → zip, and the two zips must be IDENTICAL, not merely agreed. The drill's zips hold things the station cannot yet produce, so a failure there is either a decompiler gap or an E3 backlog item and the number cannot tell you which; the fixpoint asks only the second question, and reports the sentences the compiler read WHOLE apart from the rest, because a word the zip never held is not a word the decompiler dropped. Row names, `and`-chain nesting and `unplaced` are normalised first — zip-local (req 64), associative by mathematics (req 41), and never compared (req 21) respectively. **24 → 59 of 87 in one day**, and five of the defects it found were the COMPILER's, not the decompiler's: the tense (req 25), the voice (req 26), a clause left as a claim after it became an attitude, a bare `ccomp` read as a conjunction unless the verb was in `SAYING_VERBS`, and **a zip that came out a DAG where a sentence is a tree** — clauses were joined pairwise by name, so one in two joins was named by both and the decompiler said the shared half twice. Coordination binds tighter than subordination, and that is what decides where a new join goes. Record: `202609201100_the-station-against-itself.md`.]** **[FOURTH SLICE, 2026-09-21 — **the check and the lookup must ask the same question.** `db/0028` gave the fused quantifiers a voice («nobody» · «everyone») and cost the fixpoint five sentences: the decompiler's spoken index is keyed on `(role, compiled)`, `no` and `nobody` compile to the SAME thing, and five flags overwrote three occupied slots in a dict without raising — seventeen flagged rows, twelve keys, and the count was read by nobody. The fix is that they are not the same kind of word: every quantificational row whose `word_class` is not `determiner` moves to **`fused_quantifier`** (the table always knew — only `role` failed to say so), and the index gained the `sort` so that it and the migration's own check key on the same three things. **The compiler needed no change**, which is the evidence the seam is real: `_bare_quantifier` routes on `compiled.kind` (what the word MEANS) and the decompiler on `role` (what kind of word it is). The ratchet is a count — every form curation gave a voice must be REACHABLE — because a dict does not raise. Fixpoint back to 65 of 87, gate unmoved at 65·5·17. Record: `202609210900_the-check-and-the-lookup.md`.]**
10. **The fifty-sentence drill is the acceptance gate** — **[EXTENDED 2026-09-15: the drill is the FORMAT's gate and stays one; the STATION's gate is UNIVERSAL DEPENDENCIES — every one of the 37 relations and 17 POS tags handled consistently against UD's own examples. Our sentences encode our habits; UD's are strangers'. And the Captain's 583 stored v1 journeys are NOT a gate at all — they are E9's no-regression ratchet material, usable here as evidence only. **OPENED 2026-09-15: 25 cases over 16 of the 37 relations — 16 answered · 0 WRONG · 9 abstained. `tools/ud_gate.py`, offline by default. Only WRONG is a defect; abstentions and the 21 unreached relations are reported by name every run. **COMPLETED 2026-09-16: every one of the 37 is reached.** 43 cases · 30 answered · 0 WRONG · 13 abstained; 34 relations have a case, 2 do not arise in English (`clf`, `dislocated`) and 1 is UD's own abstention (`dep`) — three states, never two. **And the gate now scores the ZIP rather than the table**, because the unreached relations were almost all compiler questions; it needed a per-token placement trace first, and found four defects with it before a new case was added. Record: `202609161050_the-gate-reaches-every-relation.md`.**]** — the no-abstention coverage bar set in advance, drilled before any officer builds.

11. **The station consults the dictionary for SHAPE, never for MEANING** *(added 2026-09-15)* — the ported content lists become geometry with a nearest-anchor fallback, so «is this an implication verb?» is answered by the base rather than by a list; but the sense slot is emitted **OPEN** and the station never picks a sense. Binding is the evaluator's one algorithm (evaluator req 5), which has a KB to check itself against and the station does not. **[AMENDED 2026-09-16 — «by the base rather than by a list» holds; «by the base's GEOMETRY» does not, and it was measured rather than argued. The shape question the markers ask is *what kind of thing is this*, which is TAXONOMY, and the dictionary chapter's standing ruling is that cosine is co-occurrence and not hypernymy. Benched on 52 cases, nearest-anchor geometry scored 6 with 3 wrong against the supersense's 11 with 0, was MUTE on 14 of 30 (most everyday nouns are not among the base's 4,555 dimensions), and made the independent bucket WORSE when placed behind the supersense as a fallback. So the instrument is a fact the resource STATES, not a distance it implies — see req 15. Record: `202609160929_the-ambiguous-markers.md`.]**

12. **The 37 → 18 mapping has TWO HALVES, and they must not collide** *(found by the gate, 2026-09-15)* — a role that arrives with a MARKER (`location`, `time`, `instrument`, …) is the closed classes' business and lives in rows; a role that arrives by RELATION (`agent`, `patient`, `recipient`) is the compile core's and comes from `nsubj`/`obj`/`iobj`. The table proves the split by holding **no marker for `patient` or `experiencer`** — there is none in English. Asking the marker table for a relational role is a category error, and it was made twice in the gate's own first transcription. **[EXTENDED TWICE, 2026-09-22.** *A THIRD CATEGORY: a marked phrase that frames the CLAIM rather than filling a box.* The station had never raised a `DomainRow` — the schema held the fifth prefix element since E2, the decompiler could say one, the drill hand-compiled three and rules reqs 6-7 depend on it, and `grep DomainRow tk2/language/` was empty. Built on two signals measured clean in BOTH directions (`tools/domain_bench.py`): a fronted `as`, and a fronted phrase whose BOX IS CONTESTED. The obvious rule — «fronted is a domain» — scored five false positives out of six, and a domain nobody stated indexes a claim to a context it was never held in, which the evaluator would then never contradict. **And one pair is irreducible**: «Legally, in Italy, he is still married» (DOMAIN) and «In France, I ate well» (LOCATION) are identical on every fact the parse gives, so the station abstains and `aw-17` stays red honestly. Record `202609221100_the-domain.md`. — *AND THE MARKER HALF ITSELF*: «as far as» was the one gap in a complete family (`as for` · `as of` · `as to` · `up to`), so «I walked as far as the bridge» came back «I walked.» **Both instruments called it green** — FIXED at the fixpoint, «no common ground» at the gate. `db/0033` supplies the row; the row ALONE made it worse, because longest-first matching consumed the `as` that was doing the work, and stanza reads the phrase two ways so the nominal's marker is sometimes not its child at all. Reading the marker's SPAN holds under either analysis. Record `202609221300_as-far-as.md`.]**

13. **A joining word says whether its clauses are CLAIMED** *(closed classes v4, `db/0010`, 2026-09-15)* — «if» and «because» are the same `IMPLY` and differ only in assertion status (req 38), and nothing in the dependency tree separates them: both are `mark` on an `advcl`. So `asserts` is knowledge about the word and lives in the row — `both` · `neither` · `matrix` (a POV: the report is claimed, its content is not) · `ambiguous` («when», where only the theatre decides, and the station abstains). **The root is not claimed by being the root**: «if it rains I stay home» asserts neither half. **[AMENDED 2026-09-18 — `or` and `nor` assert NEITHER (`db/0017`, the Captain): a disjunction claims the disjunction, not its halves, and `nor` claiming both halves contradicted its own join. Free choice under a modal («can be X or can be Y» meaning both are possible) is the exception, and it is not built.]**

14. **A wh-word says WHICH slot it opens, and three of the five kinds are not boxes** *(closed classes v5, `db/0011`, 2026-09-15)* — `box` (when → time, where → location, how → manner) · `participant` (who/what: **the relation decides**, req 12) · `truth` (whether: the polar question) · **`antecedent` (why: there is no cause box to open — req 37 — so it asks for an unknown ROW implying the one asserted)** · `field` (whose: the possessor lives inside the record, req 26). **The same word asks in a root clause and describes in a relative one**, and only tk1's R5 walk separates them: nothing in the dependency does. **[AMENDED 2026-09-16 — THERE ARE THREE READINGS AND THIS LINE NAMES TWO.** «I know WHO did it» is neither: an embedded question opens its slot exactly as a root one does, and only the utterance's MOOD differs. R5 answers the mood question rightly and was then read as «therefore relative», which left `who` unplaced. **UD does mark this one**, contrary to the line above: a relative clause modifies a NOUN (`acl:relcl`) while an embedded question is a clausal COMPLEMENT — so R5 decides the mood and the clause's own dependency decides between describing and asking. A wh-word with no free-relative row falls back to the INTERROGATIVE reading, not to whatever is first: `why` has none, and an embedded «why» still asks.]**

15. **What a marker means is settled by the SUPERSENSE of the words around it, and the rules are curation** *(closed classes v6, `db/0012`, 2026-09-16)* — «I swam in the pool» and «I left in May» differ in nothing a dependency tree can see, and everything in what kind of thing the nominal IS. WordNet files every synset under one of **26 noun and 15 verb lexicographer classes**, published and closed, so the table from a supersense to a tkzip role can be **complete** — the same argument that made UD the station's gate (req 10), arriving a second time at the other end of the mapping. The PRIMARY sense only (`db/0006`'s rule; a max over senses reads `pool` as a TIME). **`of` asks no dictionary at all** — the head's POS settles material from possessor, and the possessor is a FIELD (req 26), not a box. **A DEFAULT IS AN ANSWER AND IT IS COUNTED**: req 8 forbids the *silently* complete nearest fit, so an unchosen marker fills its box from the curation's best-first order and the zip records that nothing chose (`Compiled.defaulted`, req 4's input). **Named debt**: the supersense is read live from WordNet and sits outside the base's build fingerprint; its home is a `lexname` on `SenseVectorDoc`.

16. **A NAME IS ITS OWN POINT, AND IT IS FIVE QUESTIONS AND NOT ONE** *(the Captain, 2026-09-16, reopening the parked hole — E3b)* — *is this a name at all* (casing is an indicator and not a test: tk1's gate both admitted OOV gibberish spaCy labelled `GPE` and refused «kotekino», which is lower case and known) · *a name of WHAT* (person · public figure · organization · place · work · event) · *WHICH one* (Paris, France against Paris, Ontario — tk1 logged this and never solved it) · *have I MET it* (recognition against a names table, which is a different question from identification and is the one that carries identity) · *what it IS in a zip* (a type-centroid SEMANTIC vector plus a context-scoped IDENTITY uid, never merged). **The geographical half is CURATED and inherited** — tk1's places table, ~4.7M author-curated rows with two containment chains and a closed ~21-value `type` column that is an `is_a` statement, read lazily and never materialized. **The prominence half cannot be curated and is the ecosystem's first micro-nn** (micro-nn req 9). **An NER label is a CANDIDATE, never a verdict** — which is exactly what the micro-nn fence is allowed to rank. *This does NOT reopen req 2: entity recognition is not a UD task, has no UD relation, and stanza ships its own NER processor — so «stanza against spaCy on the same text» is a measurement E3b owes rather than a second opinion on a dependency parse.* Record: `202609160959_the-name-question.md`.

17. **THE KINDS OF NAME ARE OUR OWN INVENTORY, AND THEY ARE ROWS — NOT CODE, AND NEVER A THIRD PARTY'S** *(the Captain, 2026-09-16, answering open question 14)* — *«with our inventory as db (not code!), we can modify it keeping the logic: everything is KB. If we rely on a third party we lose this ability.»* **That is the first standing law's FRAME test, applied to a set that feels like law and is not**: «public figure» against «person» is a judgement about the world, revisable, and a roster needing a code change to grow would put the table back in code — the precedent is `ClosedClassDoc.word_class`, a string and not an enum for this exact reason. So BOTH halves are curation: the kinds themselves, and the map from an NER label to a kind. Only the reader is frame. **An NER label set could not be the frame anyway**, for four further reasons: seven of OntoNotes' eighteen (`DATE TIME PERCENT MONEY QUANTITY ORDINAL CARDINAL`) are not names but measurements the format already says, so adopting them gives tkzip a second way of saying what it has (the defect req 37's collapse was avoided for) · three (`GPE LOC FAC`) are one question for us, «is it in the places table» · **a kind must be reachable with NO label at all** («kotekino» gets `PROPN` and no NER type — tk1's recorded failure, repaired only by bolting on a second path) · and it is the shape the station already uses twice, at req 12 and req 15. **Prominence is a SCALAR on the resolution, not a kind**: «public personality» is a person who is prominent, and a company is an `organization` that is prominent. Made a kind, it would have to be decided before the thing is identified and would leave the micro-nn nothing to rank; made a number, it IS what the micro-nn ranks (req 16, E3b task 5). Record: `202609160959_the-name-question.md`.

18. **THE TWO GATES MUST MEET** *(found 2026-09-16, the hard way)* — the drill is the FORMAT's gate and validates hand-written zips with **no parser involved**; the UD gate is the STATION's and compiles UD's sentences without ever looking at the drill. **Nothing compares what the station PRODUCES with what the Captain hand-COMPILED**, and the cost was concrete: the compile core put `topic` where E2 had ruled `patient` for every copular subject, contradicting 43 hand-compiled rows, and no test went red for a day. The control that closes it: **for the drill's own sentences, does the compiler produce the drill's own zip?** Not a pass/fail on all 78 — most need features E3 has not built — but a per-sentence diff, so a role the station invents against the format's own record is visible the day it appears. *A gate that cannot disagree with the other gate is two gates measuring one thing twice.* **[BUILT 2026-09-16 — `tools/drill_gate.py`. It scores AGREEMENT rather than equality (AGREED · DISAGREED · MISSING, and only DISAGREED is a defect), pairs rows by predicate and boxes by HEAD, and never calls a VARIABLE a conflict. **On its first run it crashed the compiler on six of the Captain's own sentences** — a `SPACE` token outside UD's seventeen, and two rows called `m0` in a sentence carrying both a modal and an attributive adjective, which no UD example does. Then **six disagreements: two curation errors (`db/0015` — a marked phrase has an endpoint, so no role_marker may produce `direction`), one E3b, and three witnesses of ONE open question — the subject's role depends on what is predicated of it.** 38 agreed · 4 DISAGREED, held as a ratchet. Record: `202609161502_the-two-gates-meet.md`. **[WIDENED A FOURTH TIME 2026-09-19 — THE PREFIX. The gate read `kind="content"` and nothing else, so every attitude's holder, verb, addressee and strength, every negation, modality, domain and quantifier row, and every JOIN's truth slot were outside the instrument — 89 prefix rows and 50 joins the Captain hand-compiled, unmeasured. It was widened BEFORE the `strength` it was to measure was built, on the Captain's «widen the gate now, then docs first», because a value no instrument can contradict is not a measurement. A prefix row is resolved to the row it SCOPES (no name is ever compared), pairs on what it IS — an attitude on its verb, a quantifier on its restriction — and is compared on what it SAYS; a drill row nothing paired with is MISSING unless the station has an unpaired row of the same kind over the same target, which is a SUBSTITUTION and a defect. **No magnitude is ever compared**: `strength` is stated/unstated, as truth is. It found the imperative's empty strength and, unlooked for, a COIN TOSS in the station's own reading — see req 22. Record: `202609190900_the-gate-sees-the-prefix.md`.]**]**

19. **AN ADVERB'S SCOPE IS CURATION, AND CURATABLE ONLY BECAUSE MANNER IS THE DEFAULT** *(`db/0013`, 2026-09-16 — the four-way split of tkzip req 23, made operable)* — **the resource cannot answer it**: WordNet files every adverb under ONE lexicographer class (`adv.all`) and adverbs have **no hypernyms at all**, so both the supersense of req 15 and R's taxonomy are silent. What makes an OPEN class enumerable is that **79% of English's 3,767 single-word adverbs are `-ly` and describe the action**, so the rows hold the 106 EXCEPTIONS — `epistemic` → the prefix's modality (the adverbial spelling of `may`/`must`) · `evaluative` → an attitude whose holder is the speaker, and therefore an ABSTENTION until the POV can carry it · `discourse` → a JOIN with the row before it, abstaining when there is no row before it (the other half is context, an argument by req 7) · `circumstantial` → a box, and `roles` says which. **A default is COUNTED, never silent** (req 8's distinction, as req 15 drew it for the markers). **THEY LIVE IN THEIR OWN COLLECTION** because the closed-class forms filter D's vocabulary and sixty new ones would desynchronise the sealed base — the cost of two rosters is paid by making only the ROSTER second: `compiled` holds `ClosedClassDoc.compiled`'s own vocabulary, so a reader cannot tell which table answered, and the rows merge upward the day the exclusion set stops being load-bearing. **A MARKED NOMINAL OUTRANKS A BARE ADVERB** for the same box: «left EARLY in the MORNING» has two time expressions and one box, and the speaker chose the marker. Record: `202609161251_the-adverbs.md`.

20. **⚑ THE PERSON AXIS ROTATES UNDER A POINT OF VIEW, AND NOTHING IN tk2 KNOWS IT** *(the Captain, 2026-09-16 — asked, answered NO, and recorded)* — the blueprint's only sentence on the subject is the second standing law's *«a pronoun is indexical, so it is resolved to an ENTITY at parse time»*, which says what to do and never says that **the context it resolves against CHANGES inside a POV**. The rule: a first-person pronoun names the HOLDER of the innermost POV it is under, a second-person one names that POV's ADDRESSEE, and with no POV the outer speech act supplies both — «John said to Marie: YOU are a clever girl» means Marie, and «John thinks I am wrong» still means the speaker, because a thinking POV addresses nobody. **Four separate gaps**: there is no person axis at all (`compile()` takes no context argument — req 7 is written and not built); the axis must ROTATE, and **the addressee is a slot tkzip does not have** (`Pov` holds holder · verb · strength), so where it lives is a FORMAT ruling under a frozen schema (req 73); **stanza splits a quote into a separate sentence** and the station has no multi-sentence path, so the rotation information and the pronouns it governs arrive in different skeletons; and **neither gate tests it** — the drill has no quoted speech at all, and this QM transcribed `ccomp` as the INDIRECT «he said that…» while passing over UD's own direct-quote examples on the same page. **A wrong rotation is a wrong BELIEF, not a wrong parse**: the zip is well-formed, internally coherent, and about the wrong person, so the evaluator cannot catch it. **[BUILT 2026-09-16, the same day it was asked. The Captain ruled the FORMAT gains the field — `SCHEMA_VERSION = 3`, `addressee: Box | None` on `Pov` and `AttitudeRow`, the first migration of the frozen schema (tkzip req 73) — rather than let the resolver read a `say` row's recipient, which would have put a closed set of verbs in code. EMPTY is the point and not `Open()`: «no addressee» and «addressed to somebody unknown» are different facts, and only the first stops a rotation, which is what keeps «John thinks I am wrong» meaning the speaker. The rotation is decided BEFORE the clauses are compiled, because a pronoun resolves where it is met and its attitude is built later. And across a sentence boundary a FRAME — a saying with an agent and a recipient — rotates the sentence after it, which is what makes «John said to Marie "You are a clever girl"» mean Marie. STILL OWED: the quote becoming the CONTENT of the saying, and cases in both gates. Record: `202609161544_the-rotation.md`.]** Record: `202609161349_the-person-axis.md`.

21. **A QUESTION BELONGS TO A STATEMENT, AND THE `?` DECIDES WHICH** *(the Captain, 2026-09-18 — E3 task 2c)* — «Is the cat hungry?» compiled at truth 1.0, a question stored as a belief. **The `?` token decides**: with it the statement asks, without it nothing does, and `!` is not `?`. Word order is not read — it cannot see «The cat is hungry?», whose syntax stanza reads CORRECTLY as declarative, because English writes that question only in the punctuation. **Main, coordinate and quoted clauses are statements**; a subordinate clause belongs to its statement and asks only through its own word (`whether`, req 14). Stanza attaches the `?` to the root, so the station gives it to **the last statement it closes**; one that already asks through a wh-word keeps its truth. A missing `?` is the pre-input layer's to restore, not the station's to guess. **Frame**: the `?` is the decoded tree's punctuation, and decoding is frame (root `CLAUDE.md`, 2026-09-18). **[AND THE SUBORDINATE SPELLING, 2026-09-18 — req 14's «`whether` opens the truth» had never fired: `mark` always read the subordinator row. The rows hold both readings (`db/0016` gives `if` the one `whether` had) and the CLAUSE picks — a complement (`ccomp`, `csubj`) asks, an `advcl` supposes. Frame, the tree's shape.]** Record: `202609180900_the-polar-question.md`.

22. **THE SUBJECT'S ROLE IS SETTLED BY WHAT IS PREDICATED OF IT, AND THE RULE IS ROWS** *(2026-09-18 — ten drill-gate witnesses, one question)* — `nsubj` is not a role: «the cat CHASED» is an agent, «I LOVE» an experiencer, «God EXISTS» a patient. **Copular adjectives separate perfectly** by the supersense of their related noun (WordNet files almost every adjective under `adj.all`, so the noun is read — *hungry* → *hunger*, `noun.state` → experiencer; *green* → *greenness*, `noun.attribute` → patient); a copular noun is a patient. **Verbs do not separate by any published instrument** — the supersense contradicted itself in the drill three times and VerbNet disagreed with E2's conventions on motion verbs — so the rule is a supersense table plus counted EXCEPTION ROWS (req 19's shape): `emotion`/`cognition`/`perception` → experiencer · `stative` → patient · otherwise agent · `disagree` → experiencer. **A lone `iobj` is abstained** — UD reserves it for the double-object clause, so «I trust YOU» labelled `iobj` is a provider mislabel and a recipient invented there would be wrongly-understood (req 8); a clausal complement counts as the other object. *Perception is a class by the Captain's ruling; «look», «watch», «listen» are acts and will need exception rows when a witness arrives. `develop` is a named miss: its role follows the SENSE, which the station never picks (req 11).* Knowledge, in `language_subject_roles` (`db/0018`), run by the marker selector's probes. **Known limit**: stimulus-subject psych verbs («the dog FRIGHTENS me» — `verb.emotion`, and the dog is no experiencer) will need exception rows when a witness arrives. Record: `202609181400_the-subject-role.md`. **[AMENDED 2026-09-19 — THE RESOURCE MUST BE READ WHOLE, AND ABSTAINED FROM WHEN IT IS OF TWO MINDS. NLTK stores a synset's pointers in a `set`, so `attributes()` comes back in the process's string-hash order; *dead* has two attribute nouns — `animation.n.01` (`noun.state`) and `animation.n.02` (`noun.attribute`) — and `[0]` made «the cat is dead» an experiencer in one process and a patient in the next. **The same sentence read as two different thoughts is worse than read wrongly twice.** `derived_supersense` now reads the whole relation and answers only when it agrees: WordNet publishes no priority among a synset's attributes, so an order imposed by us would be our invention in the resource's clothes — the provider-defect doctrine, at the dictionary's end. The cost is *happy* (`noun.state` + `noun.feeling`), which becomes a LEMMA EXCEPTION ROW (`db/0019`) because `noun.feeling` is refused by «Be QUIET!»'s witness. Found by the gate's fourth widening, and it is the cause of the 2026-09-18 test that would not reproduce. Record: `202609190900_the-gate-sees-the-prefix.md`.]**

23. **HOW STRONGLY AN ATTITUDE WANTS IS KNOWLEDGE, AND THE TREE STATES NO NUMBER** *(the Captain, 2026-09-19, answering task 2d's first parked question)* — the drill writes `strength=0.9` on «Close the door!»'s POV(me · want) and 0.6 on «I WOULD LIKE to know…»; the station wrote nothing, because no number is in the tree. **Knowledge**: the gradation from «close the door» to «would you mind closing the door» is a revisable fact about how English softens a wanting, and it lives in rows (`db/0020`) with a counted DEFAULT for the bare imperative — *«the heart can influence the base value, staying on the knowledge, but let's see when we do the heart»* (the Captain). **A softener enters on a witness, not on a principle** (the 2026-09-18 law): `please`, `kindly` and «would you mind» have no drill sentence, so they have no row, and the table ships with what the drill witnesses. **The gate compares the SLOT, never the value** — `stated` against `unstated` — so a curation change is a migration and not a red gate (req 18's fourth widening).

24. **AN IMPERATIVE DOES NOT COLLAPSE INTO THE SPEAKER'S OWN ATTITUDE, AND WHICH ONES DO IS PER-VERB** *(the Captain, 2026-09-19, answering task 2d's second parked question)* — «Suppose the cat is hungry» is hand-compiled by the drill (`aw-20`) as the SPEAKER's own supposition, one attitude; the station reads it as any other imperative, *I want YOU to suppose it*, two attitudes nested. **Both are readable from the same tree, so the tree cannot settle it** — and it is not a property of the imperative, since «Remember that I love you» plainly does not collapse. It is therefore a property of the ATTITUDE VERB: knowledge, and it belongs to req 55's open classification, which E3 has not opened. Until then the station does not collapse, and `aw-20` stands as a NAMED divergence. **What the gate actually reports of it, measured 2026-09-19, is smaller and more honest than the divergence sounds**: the station builds NO attitude for «suppose» at all — it compiles a content row `suppose.v` with the addressee as its agent, ANDs it to the cat's row, and wraps the imperative's want around that — so the drill's `suppose` attitude is reported MISSING over the cat's row, not contradicted. The collapse question cannot even be asked of the station until req 55 gives an attitude verb its row. **[TWO MORE EXCEPTION ROWS, 2026-09-20 (`db/0025`) — and they are the witnesses this requirement said it was waiting for.** `look` is `verb.perception` and its subject is an AGENT: the requirement already named it (*«perception is a class by the Captain's ruling; «look», «watch», «listen» are acts and will need exception rows when a witness arrives»*) and the witness arrived the moment the station stopped compiling «He looked up» with an OPEN subject, because the gate abstains on an OPEN filler and had nothing to compare. `die` is the other: nobody performs dying. Volitionality is in no published resource, which is precisely why these are lemma rules.]**

25. **THE STATION WAS DROPPING THE TENSE, AND A SENTENCE SPOKEN BACK IN THE WRONG TENSE IS NOT THE SAME SENTENCE** *(2026-09-20, found by the fixpoint)* — a `tense_aspect` word was marked covered and compiled to NOTHING, so «I walked to the station» and «I walk to the station» were the same zip, and every one of the drill's nineteen past-tense sentences came back present. **`Theatre` was written on 0 of 87 zips.** The fix is structural, like `Mood=Imp`: UD puts `Tense` in the features of the FINITE word — not of a participle, whose `Tense=Past` is its own form and had been putting every passive in the past — and the future is a WORD whose row has said `tense: future` since closed classes v1. The interval's time axis is relative to the utterance, **-1 before · 0 at · +1 after**, which is the convention the drill's own forecast already used (`aw-22`). `THEATRE_EPOCH` stamps the reading's version (tkzip req 63). The decompiler reads it back, which is what `VBD` and `VBN` are for (`db/0024`). *`compile_utterance` was dropping it on the way out, along with req 27's marker: both were one field on the zip, so a merge had to choose, and it chose neither.* **[AND THE FIELD MOVED ONTO THE ROWS THE SAME DAY — schema v5, the Captain's ruling.** The tense is computed per CLAUSE HEAD now, not once for the root, and a clause that dissolves into an attitude takes its time with it. The merge needs no choice at all any more: every clause of every sentence keeps its own. `topicality` still has one, and still takes the first sentence's.]**

26. **REQUIREMENT 27's MARKER WAS DEAD DATA — the voice was never written down** *(2026-09-20)* — tkzip req 27 says the zip records that the speaker foregrounded the patient *«so the renderer speaks it back in the voice it was heard»*, and nothing had ever written `topicality`: it was set by hand on ONE of the drill's 87 zips and by no code at all. Read from `nsubj:pass`, with the role that phrase filled taken from `Placements`, which already says where every token went. **And the decompiler says the passive back** — `be` plus a participle, the agent marked by the word the parse recorded (req 65). *An OPEN agent in a plain claim is the same construction with nobody in the `by` phrase: «The hammer is made of titanium» does not ask who made it, and speaking it as a question would be inventing one.*

27. **⚑ AN OPEN SLOT CANNOT SAY WHAT IS KNOWN ABOUT THE THING THAT IS NOT KNOWN — and three separate defects are that one gap** *(2026-09-20, for the Captain: it is a FORMAT ruling under the frozen schema, tkzip req 73)* — `Open` carries a `prior` and nothing else, so: **«who» and «what»** compile identically, and the decompiler cannot say which was asked; **«he» and «she»** compiled identically until the station started keeping the pronoun's own key, because the sentence's person, number and gender reached no field; **«its cubs»** is a possessor in exactly that state. **[RULED AND BUILT THE SAME DAY — SCHEMA v4, and the rule above it rewritten.** The Captain: *«English says several different things with an unknown, the zip should record several types of unknown (carrying genre, plurals): the schema should be changed — not sure why you talk about "frozen" schemas … there are no frozen schemas :)»* `Open` gains `sort` · `person` · `number` · `gender`, carrying exactly what the closed-class row it matched already records, so the compiler copies and the decompiler matches back. **It came out cleaner than the stand-in**: three kinds of unknown separate with no heuristic at all — a `sort` was ASKED («who»), a `person` was DESCRIBED («she»), and a bare `Open` is a hole the passive leaves out. The rule the decompiler had been using — *an agent, in a claimed clause, not under an attitude, is probably a passive* — is deleted. **And the shortcut was fixed in the same direction**: the drill held `he.n` in thirteen boxes, a pronoun key standing for a referent, which its own doctrine calls an unresolved reference rather than a reading. *The stand-in had earned its keep first: it exposed two subject-role defects the gate's abstention had been hiding (`db/0025`) — and widening the gate to read an OPEN's features exposed two more, `q-6` and `t-dc-5`, which are ANAPHORA and are parked in **E3b**, on which they depend: nothing knows that Marie is feminine and John is not.]**
