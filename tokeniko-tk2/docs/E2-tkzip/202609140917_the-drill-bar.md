# tkzip — THE DRILL'S BAR, DECLARED IN ADVANCE, 2026-09-14 09:17

> **This file is written and committed BEFORE the drill exists.** §8 test 4 requires the coverage bar
> to be set in advance, and the E1 lesson is that «declared before the run» must be answerable from
> the ledger rather than from memory. Nothing below may be edited after the drill runs; a change
> after the fact is an amendment, dated, with the reason — never a quiet edit.
>
> *Rulings taken 2026-09-14: the bar, the size, the deliverable, the traffic source.*

---

## THE BAR

| | |
|---|---|
| **no-abstention** | **≥ 90%** of the drill's sentences must zip with nothing left unplaced |
| **no-silent-wrong** | **100%** — a single silent-wrong fails the whole drill, at any coverage |

The asymmetry is deliberate and is parser-compiler req 8: *half-understood is legal, wrongly-understood
is the sin.* A partial zip, honestly marked, is a **pass**. A confident zip that says something the
speaker did not is a **failure even at 100% coverage**.

## THE SCORING RULES — declared with the bar, for the same reason

**PASS.** Every content element of the sentence lands in a slot, and the zip's structure is the
sentence's structure. Nothing unplaced, nothing invented.

**PARTIAL (counts as an abstention, against the 90%).** Some element could not be placed, AND the zip
says so honestly — an `OPEN` slot, the unplaced-material diagnostic, confidence lowered. The reading
is incomplete and the zip admits it.

**FAIL (silent-wrong).** The zip is complete and confident and says something the sentence does not:
a claim the speaker never made · a distinction conflated · a negation, scope or modality lost · two
different sentences producing the same zip when they mean different things.

## THE HONEST-INSTRUMENT GUARD

The drill may **force schema changes** — that is what it is for, and the Captain ruled at task 1 that
a missing slot is *«a bug, found by the instruments, fixed by redesigning the frame»*. A cluster that
decides something is the drill working, not the drill failing.

But an instrument that can always be satisfied by moving the target measures nothing. So:

1. **Every schema change the drill forces is RECORDED**, with the sentence that forced it.
2. **The COUNT of forced changes is reported beside the final score.** A drill that scores 100% after
   forcing fifteen changes says the conception was weak — and that is the honest reading, not a
   footnote.
3. **The drill is re-run whole after any change.** No per-sentence patching.
4. A defect that survives the fold is a **FAIL**, not a pending change.

---

## THE SIZE AND THE ALLOCATION

**Seventy-five sentences** *(raised from fifty on 2026-09-14: fifty was sized before E2 required six
clusters, which alone consume ~28 and would have crowded out the real-traffic half — the half most
likely to find what we did not think of).*

| half | count | source |
|---|---|---|
| **the clusters** | ~30 | ruled during E2, each because something is genuinely undecided |
| **real traffic** | ~30 | tk1's stored journeys — sentences he actually heard |
| **awkward by design** | ~15 | README §6's list: nested attitudes, `source`, secondary predication, two quantifiers, three operators, passives |

## THE SIX REQUIRED CLUSTERS, and what each must DECIDE

| cluster | the question it exists to answer |
|---|---|
| **«only»** | is it a **sixth prefix element**, or does it collapse into IMPLY→EQ plus binder restriction? *(task 2's one stated doubt)* |
| **«to / toward / into / onto / as far as»** | is **`direction` a MISSING BOX**, distinct from `destination`? PropBank (`ARGM-DIR`, 1,419) and FrameNet both keep it separate *(task 5, case 7)* |
| **existential `be`** | «there is a cat» — content, not copular glue *(task 1)* |
| **`never` · `hardly` · `almost`** | structure, not description — «he almost died» asserts he did **not** *(task 2)* |
| **«twice a week»** | does `count` really hold it, or does `frequency` come back as a box? *(task 2's least confident ruling)* |
| **de re / de dicto** | does the prefix actually express both, as α claims? *(task 2, case H)* |

## THE TRAFFIC SOURCE

`tokeniko_mem.tkzipdebug` — **583 stored journeys, 245 judged mismatches.** Read-only; the biography
is not touched. *(Permission given 2026-09-14: «of course that's the right thing to do».)*

Of the 245, **114 are FORMAT failures** — the categories where v1's schema, not its senses and not
its ears, was the thing that failed:

| category | count |
|---|---|
| wrong-structure | 59 |
| dropped-content | 22 |
| missed-mood | 12 |
| operator-flattening | 10 |
| missed-negation | 8 |
| missed-modality | 2 |
| missed-quantifier | 1 |

**Excluded on purpose:** `wrong-sense` (84) is the evaluator's problem, not the format's — the parser
emits the sense slot OPEN by ruling. `ears-hallucination` (27) and `ears-translation` (2) are rag's.
Drilling them would measure the wrong layer and flatter the schema.

**Selection rule, declared now:** the ~30 are drawn to cover **every one of the seven format
categories**, weighted toward the unaddressed leads, and chosen for **structural variety** rather than
for being easy. Sentences already known to break the schema are explicitly welcome — a bar that only
admits sentences we can zip is not a bar.

## THE DELIVERABLE

- **`tk2/tkzip/schema.py`** — the shape as pydantic. It is **frame** under standing law 1 (the shape
  in which anything can be stated at all), so it belongs in code and moves only by migration.
- **`tests/fixtures/drill.py`** — every sentence hand-compiled to a literal zip, which must validate
  against the schema and round-trip through the storage form.

A schema that exists only as prose is describable, not frozen. This makes the freeze real, makes E3's
job «produce these zips from surface text», and makes every later change a migration against
something executable.

---

*Declared before the drill. `git log` is the proof.*

---

## AMENDMENTS

*The doc's own rule: «a change after the fact is an amendment, dated, with the reason — never a quiet
edit». The BAR and the SCORING RULES have never been amended and are not amendable; what follows
concerns the SIZE, which was declared as a floor.*

**2026-09-14 — 75 → 78.** The six clusters ran slightly long as they were drilled. No reason beyond
that; recorded because the number is declared and the ledger is the proof.

**2026-09-17 — 78 → 87: the QUOTATION block (`q-1` … `q-9`), added to the AWKWARD half.**

Schema v3 gave `Pov` and `AttitudeRow` an `addressee` on 2026-09-16, to carry the Captain's own
question: *«"John said to Marie «You are a clever girl»" — the "you" should rotate to take for
tokeniko the meaning of marie, not himself»*. **A field the drill never exercises is a field nobody
has checked**, and the drill is where the format is held to account, so nine sentences were
hand-compiled against it.

Adding sentences is the safe direction for an instrument — it can only make the drill harder, never
easier — but it is still growth, and two things keep it honest:

1. **Every one of the nine is a case that DECIDES something**, in the same sense the six clusters
   were: quoted against reported (`q-4` / `q-5`, a `distinct_from` pair that differs by punctuation
   alone), the two persons crossing (`q-2`), depth two (`q-9`), the truth slot under a saying (`q-8`).
2. **The block was written before the station was run against it**, and it immediately disagreed with
   the station on the point `q-4` exists to make. That is the drill working. Had the sentences been
   written afterwards, they would have been written to agree.

*Outcome, same day: the station had the rotation INVERTED — reported speech rotated, quoted speech
did not — and the truth slot under an attitude was being blanked against the drill's own convention.
Both fixed; `q-4` `q-7` `q-9` now agree. **The drill moved the station, which is the direction that
was supposed to be possible and had never once happened.***

**2026-09-18 — twelve ROLES corrected, in nine sentences; the count is unchanged (87).** The first amendment that edits
hand-compiled rows rather than adding sentences, so it is argued in full.

The station's subject-role rule was benched against every subject the drill holds, and three of its
predicates contradicted each other. Asked whether they were deliberate, the QM — who compiled them,
with the Captain acknowledging — judged each against the ONE principle E2 wrote down for this
(`202609111051_notes.md`, ruling 2: *«I kicked the dog (I act) vs I fear the dog (it happens to me)…
verbs of FEELING AND THINKING»*), and the Captain acknowledged the verdicts:

    think.v   t-of-1                     agent -> EXPERIENCER   ruling 2 names thinking; the case's
                                                                 note argues the IMPLY, never the role
    live.v    t-dc-1 (x2) t-dc-5 t-ws-2 q-7  agent -> PATIENT      a state, not an act — VerbNet files it
                                                                 with `exist` (Theme -> patient, ruling 1)

**And the same verdicts, applied where the bench could not look.** The gate never compares a
VARIABLE, so a scan of every row against the rule found the rest of the family:

    think.v   nha-2 · t-of-2 (x2) · t-md-2   agent -> EXPERIENCER   the same verdict
    learn.v   t-dc-3                          agent -> EXPERIENCER   the drill contradicted itself:
                                                                      experiencer in t-ws-8 and t-dc-4
    reason.v  t-dc-3                          agent -> EXPERIENCER   thinking, by ruling 2
    perceive  t-dc-3                          agent -> EXPERIENCER   the drill contradicted itself:
                                                                      «to SEE the sea» (aw-6) is one.
                                                                      Ruled: perception is the class

**One miss named, not amended: `develop`** in `only-1`/`only-2` («a person DEVELOPS cancer») is an
experiencer and the drill is right — but «I develop software» is an agent, and the difference is the
SENSE, which the station never picks (parser-compiler req 11). A word-level exception would be wrong
half the time.

Kept, and argued: `exist` → patient (`exist-3`'s note — one shape with «there is»), `disagree` →
experiencer (`aw-16`'s note — a position held), `stay` → agent (controllable: «what I did was stay
home»). **The `live` rows all AGREED with the station.** Correcting a row the
station agrees with is the harder direction, and it is the reason the verdicts were asked for rather
than inferred from which side the gate would prefer.
