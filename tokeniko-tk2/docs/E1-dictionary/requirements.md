# dictionary review — REQUIREMENTS

*Serves: `E1` · `E1b` · `E1c` · `E1d` — the ids of `roadmap.md` / `plan.md`*

*What the geometry must respect. One line each. Evidence lives in the dated `_notes.md` beside this.*
*Status marks (review closed 2026-08-12): **PROVEN** = measured by the instrument · **RULED** = the Captain's standing decision · **BUILT** = enforced in the prototype code · **OPEN** = needs a mechanism or a ruling.*

> ### ⚠ AUDITED 2026-09-14 AGAINST THE BUILT BASE — `202609141115_the-e1-audit.md`
> Every claim below was re-measured on the sealed build `9824ef465c86`. **11 of 21 clean · 6 partial ·
> 2 lost · 1 contradicted by construction · 1 superseded.** Audit marks are appended in **[brackets]**;
> the original wording is left untouched, because what a requirement SAID is part of the record.
> New marks: **[REFUTED AT SCALE]** · **[LOST]** · **[CONTRADICTED]** · **[PARTIAL]** · **[HISTORICAL]**
> · **[SPECIFIED]** · **[DONE]** · **[HOLDS]**.

1. RULED — **One space, no categories.** Verbs are not a separate object from nouns; «land on the bed» must reach «sleep» without a verb-class partition. (The R/D split is two *layers over one key space*, not two categories.)
2. PROVEN — **`eat` stays near `food`.** No precision gain may cost an existing correct nearness; the nearness lives in D (+0.405) and survives. **[REFUTED AT SCALE 2026-09-14: the built base reads D cosine +0.0484, D direct cell 0.0000, blended +0.0180 → ABSTAIN. And the ORDERING is wrong, not the threshold: `bed~cause` (FAR) +0.047 scores above `eat~food` (NEAR) +0.018, and 11 of 16 declared-NEAR pairs fall below the highest declared-FAR pair. CAUSE, measured: their glosses share nothing — D measures what two definitions SHARE, while the real relation is that eat's definition NAMES food. **CLOSED 2026-09-14 by E1d T3**: `gloss_reference` mined into R at 0.9, and a STATED CELL may now decide (req 19, honoured at last). `eat.v~food.n` reads NEAR.]**
3. RULED — **The lexicon is fixed.** Words live in relation to each other; the KB never moves them.
4. RULED — **The movable geometry is derived** — the interpreted space of a zip; what is learned is the *map* between the two. (Built later, in the blueprint proper.) **[SPECIFIED at E2 — tkzip reqs 12, 16, 43 — not built; composition is undefined for 7 of the 10 operators.]**
5. PROVEN twice — **Consequence is learned, not looked up.** Relations give `sleep~tired` 0.000, and the definitions are silent from both sides. **[HOLDS — the built base reads exactly 0.0000 on both floors.]**
6. RULED, deferred — **The compound is the atom of the derived space** (layer two, its own session). **[SPECIFIED at E2; not built.]**
7. RULED — **Identity carries no geometry.** «me» contributes whose state is located, never what it means.
8. BUILT — **Sensitive verbs are in scope by construction** — the seeds + the bar's own words are forced into every subset.
9. PROVEN — **The base must split by POS** (measured cost ×1.70 — 341 of 574 words are multi-POS).
10. SUPERSEDED — ~~density from relations, never co-occurrence~~ refuted 2026-08-12 (`eat~food` has no WordNet edge) → **the Captain's ruling: TWO matrices over one key space** — R (named edges, signed, sparse, accountable) and D (gloss co-occurrence, unsigned, dense, topical) — consulted separately, every answer naming its source, never blended into one float. **[CONTRADICTED BY THE READER 2026-09-14: `space.py` returns `cos(R + 0.15·D)`, one float, while `distribution.py`'s own docstring says «neither is folded into the other, here or anywhere downstream». The Captain's ruling: SEPARATE — «that was a mistake; blending is against what we thought and we did for a reason». **DONE 2026-09-14 (E1d T2, policy v11)**: R decides, D proposes, ABSTAIN when R is silent — measured, since where R is silent D cannot separate a declared NEAR from a declared FAR.]**
11. FACT — **The base is already explicit and hand-editable** (2925×2925 full squared matrix); the *dictionary* is not, being defined by reference to the base. **[HISTORICAL — describes the Jurassic base. The built base is R+D sparse with per-cell provenance: inspectable, but an edit is a migration.]**
12. BUILT — **The bar is set before the matrix is built** — `PAIRS` declared from the dialogue; editing a pair after a result must be said out loud.
13. RULED — **Derived vectors are a cache in the zip**, recomputable — so cosine runs in the DB. (Implementation phase.) **[BUILT at E2 — tkzip req 62, `GeometryCache`. The consequence: a dictionary rebuild invalidates only the cache, so E9's translation night is for a SCHEMA change and never for a new base.]**
14. PROVEN — **The antonym SIGN must survive the density** — +0.519 blended vs −0.186 relations-only on the same pair; only R keeps it (172 signed cells). **[PARTIAL — holds where R is strong (`arrive~leave` −0.246, `enter~leave` −0.126) and is LOST where D dominates: `thick~thin` is R −0.049, D +0.676, blend +0.011. Not in the bar, so nothing watched it. **DONE 2026-09-14** — separate reads dissolved it: `thick~thin` and `push~pull` recover their FAR, and R's negative is never diluted because it is never blended.]**
15. PROVEN, repair mechanism BUILT — **Membership is a defect too** (`want`, `swallow`, `chew` absent); added dimensions are declared in the manifest, never smuggled. Full-base repair OPEN. **[PARTIAL — `want`, `swallow`, `negation`, `runway` are now DIMENSIONS. `chew`, `love`, `hate`, `peckish`, `famished` are in the LEXICON (chew has 3 senses, love 10) but are not axes — so «eat entails chew» still cannot be a base-to-base R cell. The QM's first audit pass called them ABSENT; that was wrong and is corrected here.]**
16. PROVEN — **The POS split is necessary and NOT sufficient** — `land.n~land.v` still 0.410 split, re-merged by `derivational`. The fix (down-weighting that edge) is OPEN, a value review, not an addition. **[IMPROVED, NOT CLOSED — `derivational` at 0.45 brought it 0.410 → +0.1695; declared FAR, reads ABSTAIN.] [CLOSED 2026-09-14 (E1d, policy v13, `db/0006`). The proposed fix was measured IMPOSSIBLE — a weight is a monotone rescale and `compass.n~compass.v` (FAR) sits above `buy` and `cause` (NEAR) at every scale. The defect was a RESOLUTION MISMATCH: the closure reads senses="primary", so a dimension IS its primary sense, while `derivational` was mined across every sense. Mined at primary-sense resolution it separates all seven declared siblings with no threshold (24.1% of 1,626 sibling pairs linked, 0% unanswerable); on a real rebuild `land.n~land.v` falls to +0.034 and `compass.n~compass.v` to +0.095. The NEAR floor that `compass` had pinned at 0.28 was re-ruled AFTER the rebuild: +0.15 (policy v14, `db/0007`, applied), the middle of the gap between `compass.n~compass.v` +0.095 and `walk.v~run.v` +0.212, on build `969766250c02`. Bar 19 of 37, 0 wrong. Record: `202609141714_requirement-16.md`.]**
17. STANDING METHOD — **Doubts are settled by measurement**: code, drafts and tests are legitimate instruments for DEFINING requirements.
18. BUILT — **Every cell states which relation produced it** — including curated cells, which carry evidence + the Captain's authorization.
19. BUILT, vindicated — **A verdict reads BOTH the cosine and the direct cell** — first curation round flipped the cell read while the cosine stayed mute; cosine-only would have called curation useless. **[PARTIAL — both reads exist (`relations_of` returns stated cells) but the VERDICT did not use them: it read one blended cosine. **FULLY HONOURED 2026-09-14**: T2 separated the layers and T3 made the STATED CELL decide, which is what closes requirement 2.]**
20. RULED + PROVEN — **Curated edges are analytic only**, cross-referenced from either side («bed: furniture for *sleeping*» → `bed.n→sleep.v`); reciprocal cell at 0.60 (the `entailed_by` convention); the contingent stays KB. **[LOST IN THE REBUILD — the built base has ZERO curated cells (`mined` 51,564 · `axis` 4,555 · `curated` 0). Both edges the Captain approved by hand lived in the prototype db, which was dropped at E1b; the pipeline was rebuilt, the edges were not carried, and nothing checked. `sleep~bed` — the pair «only R gets right» — now reads 0.0000 on both floors. REPAIRED 2026-09-14: T4 made a curated edge an INPUT — `dictionary_curated_edges`, consumed by the build, which now REFUSES if an approved edge cannot be placed — and T5 restored both rows (`db/0005`). `sleep~bed` closes.]**
21. PROVEN — **Membership can be polluted by inflection collisions** — «something *left* over» minted `left` (the direction); gloss reduction must match POS-aware lemmas. Fix OPEN (same disease: `use.v`/`used.v` duplicate dimensions). **[DONE — `used.v` is not in the built base.]**

22. MEASURED 2026-09-19 — **A PLACEMENT IS JUDGED BY THE HALF THAT PLACED IT, NOT BY A THRESHOLD.** The acceptance floors are the BASE's, fitted on base-to-base cosines (p90 +0.000), and E3 task 6 asked the sense layer for its own. It has none to have: on **forty placements the Captain ruled** (`tests/fixtures/placement_bar.py`, drawn stratified from the 116,725 the build holds) the verdicts **INTERLEAVE** with the cosine in both halves — R's highest FAR +0.4454 above its lowest NEAR +0.2606, D's +0.3780 above its +0.1654 — so there is no gap for a floor to sit in, and the best floor possible for D is +1.0000, *trust nothing*, arrived at by arithmetic rather than by preference. **R ranks CONCENTRATION, not correctness** (when the stated edge points at a non-dimension the projection falls to siblings, and a sibling of a sibling says nothing); **D ranks the definition's GRAMMAR** — see requirement 23. **RULED AND BUILT 2026-09-19**: a placement is NEAR where the sense STATES an edge to that dimension, and ABSTAIN everywhere else — including every R placement reached by the sibling fallback, which is where all six of the bar's relational failures come from. The two-way version («R is trusted») was approved first and then withdrawn on a measurement: it would have carried six wrong trusts in twenty. **Coverage decided it** — 31.5% of relations placements state their nearest dimension, 19.4% of all of them, and D is 0.0% and cannot be otherwise, since a sense reaches D by stating no relations. `FAR` is never issued: a placement has no opposition to declare. Built in `DictionarySpace.place`. *The bar is EVIDENCE and cannot be `dictionary_bar` rows: every bar word becomes a seed (req 12), so declaring these pairs would pull the senses under test into the base as DIMENSIONS and invalidate the build they were measured on — `db/0007` wrote the warning, this is the first case that met it.* Record: `202609191500_the-placement-floor.md`.

23. NAMED 2026-09-19, OPEN — **D's gloss reading admits the definition's FRAME WORDS, and they outrank its content.** `mark.v` is the nearest dimension for three unrelated senses — *marked by attention*, *marked by sensationalism*, *marked by caution* — and so it goes for «given to», «in accordance with», «the act of». `inquiring.a.01` lands on `give.n` while `inquiry.n` sits at rank two; `strategical.a.01` lands on `concern.v` and «strategy» appears nowhere in its top three; `transcendental.a.01` («NOT IN accordance with nature») lands on `accordance.n`, the negation invisible; `fin.n.01` («the sum of FOUR and one») lands on `four.a`, off by one. **A stop-list is the obvious wrong answer** — a set in code, and the standing law's own example — and the right shape is likely req 15's: read what the resource STATES about a word rather than which words its definition happens to contain. Owed by E1. Found by the placement bar; record `202609191500_the-placement-floor.md`.

## OPEN at close — what the next session inherits

- **Sense-split seam** — **RESOLVED, 2026-09-10. [AUDIT 2026-09-14: this entry was STALE — it said
  the sense layer was not built. E1c BUILT it: 120,475 senses placed, 816,309 cells, sealed.]**
  Dimensions stop at POS: the base E1 built is 4,555 POS-split keys and nothing finer. The resource's
  120,475 senses ride ON the base (a sense holds a D-vector over
  base dimensions and consults R through its sense key, tkzip req 11) and that layer has no epic
  yet. What remains open is HOW: the derivation of a sense's vector, and sense selection
  (`senses="primary"` is the standing walk and its rot is measured — `small` reads as «the slender
  part of the back»). Still CONSTRAINED by tkzip req. 9: whatever the ruling, senses must stay
  distinguishable in the dictionary layer, since a wrong verb sense is a wrong action.
- **`land.n~land.v`**: down-weight `derivational` — the one bar miss no addition can fix (16).
- **Full membership repair** with POS-aware lemma matching (15, 21) — mechanized, awaiting the real base build.
- **The D threshold**: `walk~run` reads 0.205 in R (troponym siblings, no direct edge) — whether manner-siblinghood should be R-stated or D-carried is undecided.
- **`effect`'s vocabulary**: widened to `state_of` (ruling 2026-08-12); further curated relation types will pose the same question again.
