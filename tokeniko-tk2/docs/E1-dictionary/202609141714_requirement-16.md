# dictionary — REQUIREMENT 16, closed, 2026-09-14 17:14

*E1d, after T5. Requirement 16 was the last one the E1 audit left open. Run bench-first under the
standing instruction; the bench was audited by the Captain before any candidate was tested.*

> **Req 16, as written on 2026-08-12:** *«The POS split is necessary and NOT sufficient —
> `land.n~land.v` still 0.410 split, re-merged by `derivational`. The fix (down-weighting that edge) is
> OPEN, a value review, not an addition.»*

---

## THE FAMILY, as the bar declares it

| declared NEAR | R cosine | declared FAR | R cosine |
|---|---|---|---|
| `fast.n~fast.v` | +0.556 | `compass.n~compass.v` | +0.273 |
| `buy.n~buy.v` | +0.324 | `land.n~land.v` | +0.167 |
| `cause.n~cause.v` | +0.222 | `play.n~play.v` | +0.124 |
| | | `state.n~state.v` | silent |

**`derivational` holds for all seven**, and the bar splits them three against four. So the
discriminator was never the strength of the relation — it is whether the two readings MEAN the same
thing: `fast.n` «abstaining from food» / `fast.v` «abstain from eating» are one thing;
`compass.n` «navigational instrument» / `compass.v` «bring about; surround» are not.

## THE BENCH

| # | case | what it tests |
|---|---|---|
| 1 | can any `derivational` weight separate the seven? | req 16's own proposed fix |
| 2 | every POS-sibling pair in the base, not just the seven | is a separation real or fitted to seven? |
| 3 | pairs where the instrument is silent on both readings | it must abstain, not guess |
| 4 | does the fix change any non-POS pair? | it must be provably narrow |
| 5 | `fast` and `buy` are already decided | the fix must not lose what works |
| 6 | a word with three or more readings | the rule is pairwise; does it stay coherent? |

---

## CANDIDATE 1 — down-weight `derivational` (req 16's own proposal): IMPOSSIBLE

A weight is a monotone rescale: every pair moves together and the ORDER never changes.

```
scale    fast   compass    buy     cause    land    play
         NEAR    FAR      NEAR     NEAR     FAR     FAR
 0.25   +0.158  +0.072   +0.089   +0.059  +0.043  +0.032
 1.00   +0.556  +0.273   +0.324   +0.222  +0.166  +0.124
 2.00   +0.801  +0.467   +0.509   +0.374  +0.296  +0.220
```

**`compass` (FAR) sits above `buy` and `cause` (both NEAR) at every scale.** The fix requirement 16
proposed for itself could not have worked, and it was open for a month.

## CANDIDATE 2 — let D decide within the sibling class: FAILED ON THE POPULATION

The QM's hypothesis: when R states only `derivational`, ask D whether the definitions share vocabulary.
On the seven it separated them — lowest NEAR 0.042, highest FAR 0.022, a gap of **+0.020**.

**Across all 1,626 POS-sibling pairs in the base that line is the population median** (0.0185), with
**46.4%** of pairs above it and D silent on 38%. A threshold on the median is a coin flip. Seven pairs
were thin evidence, and case 2 of the bench existed to say so.

## CANDIDATE 3 — the sense layer: FAILED

Projected primary senses tie `cause.n~cause.v` (declared NEAR) at 0.0000 with every declared FAR pair,
on both layers.

---

## THE ACTUAL DEFECT — A RESOLUTION MISMATCH

The closure reads **`senses = "primary"`**, so a base dimension **IS its primary sense**. But
`derivational` was mined across **every** sense of the word. WordNet states derivation between
**lemmas of senses**, and it already knows the answer:

```
cause    NEAR   cause.n.01   <-> cause.v.01     LINKED
fast     NEAR   fast.n.01    <-> fast.v.01      LINKED
buy      NEAR   bargain.n.02 <-> buy.v.01       LINKED
land     FAR    land.n.01    <-> land.v.01      not linked
compass  FAR    compass.n.01 <-> compass.v.01   not linked
play     FAR    play.n.01    <-> play.v.01      not linked
state    FAR    state.n.01   <-> state.v.01     not linked
```

**Over every POS-sibling pair in the base (1,626): 24.1% linked · 75.9% not · 0% unanswerable.** A
binary fact the resource states: no threshold, no fitted number, full coverage. That is requirement
16's own sentence coming true — the split happened at the KEY level while the edge kept being mined
word-wide.

---

## THE MEASUREMENT ON A REAL REBUILD

Measured first on an approximation (cells filtered out of the sealed matrix), then **confirmed on a real
rebuild of R under policy v13**, over the sealed build's key space, with everything E1d built
(separate reads, gloss references, the two curated edges):

- `derivational` cells: **5,964 → 1,719**
- `land.n~land.v` +0.167 → **+0.034** · `compass.n~compass.v` +0.273 → **+0.095** · `play.n~play.v`
  +0.124 → **0.000** · `state.n~state.v` stays silent
- `cause`, `fast`, `buy` keep their `derivational` cell (+0.450), all three linked

| NEAR floor | decided | wrong |
|---|---|---|
| 0.05 | 20 | 1 (`compass`) |
| 0.08 | 19 | 1 (`compass`) |
| **0.10** | **19** | **0** |
| 0.12–0.20 | 19 | 0 |
| 0.25 | 17 | 0 |
| **0.28 (standing)** | **17** | **0** |

**`compass.n~compass.v` was the wall the NEAR floor was fitted against** in `db/0002`. With it at
+0.095, the best zero-wrong floor is **0.10, on a plateau that holds to 0.20** — against 0.28 on a
knife-edge 0.012 wide.

### The cost, stated before the ruling that pays for it

**At the standing floor 0.28, v13 decides 17 — one FEWER than the 18 E1d reached through T5.**
`buy.n~buy.v` falls from +0.324 to +0.246 when its spurious secondary-sense derivations go, and
abstains. The gain lands only when the floor is re-ruled. So re-ruling it after the rebuild is a real
dependency, not caution.

---

## THE RULINGS

1. **Mine `derivational` at primary-sense resolution** — ruled by the Captain («go»). A new relation
   setting, `derivational_resolution` = `primary_sense`, declared by **policy v13 (`db/0006`)**. `None`
   for v12 and earlier, which reads as the walk those versions ran and keeps their fingerprints.
   Both ends are restricted: the source through the provider's own one-synset walk
   (`relations_of_sense`, the same walk the sense layer reads, so there is no second set of decisions to
   drift), and the target in `relations._candidates`, where the column is known — a derivation lands
   only on the column whose own primary sense it names.

2. **The NEAR floor is NOT moved** — ruled by the Captain («waiting the rebuild»). The 0.10 plateau is
   measured on a rebuild of R alone over the sealed key space; the base that will exist after
   `0003`–`0006` are applied and the dictionary is rebuilt may differ in membership (the bar and the
   floors are coupled — `db/0002`'s lesson). Order: **apply → rebuild → re-measure the frontier →
   rule the floor.**

## WHAT IS STILL OPEN

- **The floor**, owed after the rebuild.
- Case 6 of the bench (words with three or more readings) is covered by the rule being pairwise on
  primary senses; it was not separately stress-tested beyond the full-population count, which
  includes every such word.
- **D's neighbourhood is noise at the top** (found at T2) — unrelated to req 16 and still unsolved.
