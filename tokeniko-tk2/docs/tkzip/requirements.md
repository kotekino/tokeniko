# tkzip — REQUIREMENTS

*What the format must respect. One line each. Evidence lives in the dated `_notes.md` beside this.*

1. **Fixed size.** Comparability in O(1) by the DUAL READ — cosine + stated cell, both O(1) — is the thesis; variable size trades it for graph matching. *(Amended 2026-08-12: cosine alone cannot state opposition — antonyms sit CLOSE in the topical geometry; see dictionary req. 19 and the map.)*
2. **Every slot carries a BINDING STATE.** Unbound ≠ 0. Unknown is a variable, never a falsehood.
3. **One shape, three readings** — all bound = assertion · one unbound = question · unresolved = equation.
4. **Storable with variables still open.** An unfinished thought is a first-class memory.
5. **An equation asserted true is supposition** — scenario reasoning must not require believing.
6. **Scope rides inside** (who · in what context), so a contextual defeat is never relearned.
7. **No act type.** A zip is *consumed* by a hardwired category; only the dispatcher is semantic.
8. **Slots must cover what v1 dropped**: coordination · restriction («only») · modality · complement subordination.
9. **Verb senses are load-bearing** — a wrong verb sense is a wrong action, not a wrong word.
10. **Sense selection should be derived, not ordered.** No hardcoded ladder: the sense that keeps the KB consistent wins.
11. **A role stores the D-side geometry and consults R through its sense key** (ruling 2026-08-12): retrieval is topical cosine over cached vectors; stated relations — sign, entailment, cause — are read from R at evaluation via the sense the role already carries. The sense-bridge survives intact. **[FLAGGED 2026-09-14 — the premise is in doubt. D's cosine was benched as a proposer and scores 5.9% precision@10 against gold (synonyms + same-hypernym siblings) with 36.6% junk; `eat.v`'s D-neighbours are `lead.v`, `bring.v`, `overfamiliar.a`, because all three glosses contain «take», and 62% of D's cells sit at the cap. NO VARIANT OF D HELPS — min_shared, uncapped and idf all land within noise. R's cosine scores 28.7% (advantaged by construction, since the gold is WordNet-shaped) and the sense projection has the lowest junk at 1.4%. So «retrieval is topical cosine over the D-side geometry» may be resting on a geometry that cannot rank — E3 must not design around it quietly. Evidence: `../dictionary/202609141853_d-cannot-propose.md`.]**
12. **The compound's point is a TRANSFORMED COMPOSITION, never a centroid** — the roles' D-vectors composed through the operators; negation and binding MOVE the point («land on the runway» ≠ «land on the opposite»).
13. **The derived layer is the figurative sphere** — analogy, metaphor, rhetoric: a separate, context-shaped, deliberately DYNAMIC layer above the fixed base.
14. **A micro-nn shapes the figurative layer** — experience and jargon keep it moving; it PROPOSES the reading, the evaluator admits it (the instinct fence holds here too).
15. **Stable figures CRYSTALLIZE into KB rows** — a dead metaphor becomes vocabulary; instinct is the dynamic front, settled figures become inspectable knowledge.
16. **Derived points are stored EPOCH-STAMPED beside the zip** — cosine finds over the stored index, the found few are recomputed at use; the sleep phase re-derives stale points lazily, never a full recompute.
17. **The dual read recurs at the compound floor** — derived-cosine plays D, KB rows play R: geometry proposes, stated knowledge gates — one law at both floors.

---

*Added 2026-09-11 at E2 task 1 — the role inventory. Evidence: `202609111051_notes.md`; the
Captain's own first draft: `origin-the-excel-draft.md`.*

18. **The role inventory is FRAME** — fixed in code, exhaustive at freeze. Language *structure* is stable where *vocabulary* is not; a forgotten slot is a bug to be found by the instruments and fixed by redesigning the frame, **never a migration**.
19. **Roles are NAMED and verb-independent** — box N means the same thing in every zip. PropBank's numbered args (`ARG2` = recipient for *give*, substance for *fill*) break comparability and are unusable. *Settles OQ2: passive normalizes to deep roles.*
20. **The slot is frame, the marker words are KB rows** — `destination` is the shape; *to · toward · into · onto* are `language_closed_classes` rows and grow.
21. **No catch-all slot, ever** — a typed `other` is the db option by the back door. Unplaced material is a **diagnostic outside the geometry**, never compared, read only by the debug tooling.
22. **Every zip carries the SCHEMA VERSION it was compiled against** — E9's translation night must know what it is translating.
23. **Adverbs split four ways by scope** — manner → a box · epistemic/evaluative → clause properties · circumstantial → a box or the quantifier · discourse → between-row operators. *(never · hardly · almost are structure, not description.)*
24. **Manner is a box; the ROTATION lives in the derived layer** — the zip stores the parts, the derived point composes them (reqs 12, 16), and the sign that separates antonymous manners comes from **R**, since D reads *relentless ≈ lazy* (antonyms sit close: love/hate 0.86). The transform is **bounded**, so a shared verb keeps two sentences comparable.
25. **The theatre is DERIVED** — spacetime is a numeric projection of the fillers, never a replacement for them: *Genoa is a city you can reason about, not a coordinate.*
26. **Every nominal box is a RECORD of seven fields** *(five at task 1; `sense` added at task 3, req 52; `marker` at task 5, req 65)* — `quantity` (universal · negated_universal · existential · negative) · `count` (the number, or none) · `determination` (definite · indefinite · generic) · `relation` (the possessor) · `noun` · `sense` · `marker`. The record is **per noun phrase**, not one per clause read off the subject (v1's limit; «all cats eat some fish» needs two), and quantity is split from determination because they are orthogonal: «**the three** cats» is definite AND counted, «**not all the** cats» is negated_universal AND definite — v1's single seven-valued field can say only one at a time. `count` is also where «twice a week» puts its number (req 32). *OQ7 answered.*
27. **Topicality survives as one non-geometric marker** — roles normalize so they compare, and the zip still records that the speaker foregrounded the patient, so the renderer speaks it back in the voice it was heard (req 9).
28. **Purpose and cause are OPERATORS BETWEEN ROWS, never boxes** — one encoding per meaning, so «for money» and «to save money» are the same zip. *OQ3 answered; the IMPLY form stays withdrawn as unsound.*
29. **Secondary predication is a SECOND ROW** — depictive co-asserted (`AND`), resultative joined by `IMPLY` with the theatre giving the order. *OQ5 answered. **Rewritten the same day at task 2**: the first form named an explicit `RESULT` operator; `RESULT` dissolved with `CAUSE` (req 37). OQ5's condition — an explicit link, since temporal succession is not causation — is met better, because implication is the link and bare succession still derives nothing.*
30. **Cause-as-participant merges into `agent`** — «the storm broke the window»: volition is readable from the filler, and a separate box would restate what the filler already says.
31. **Plain copular `be` compiles to structure** and never fills the predicate; the `complement` box holds the property, so `become` / `seem` / `remain` keep a home and «the cat is cute» is still *cat + cute, no verb*. *(Existential `be` is content.)*
32. **Frequency is QUANTIFICATION over the time variable**, not a box — *always* universal, *never* negation + universal, *twice a week* a count.

**THE INVENTORY (closed 2026-09-11) — seventeen boxes plus the predicate.**
*Participants:* `agent` · `patient` · `experiencer` · `recipient` · `beneficiary` · `instrument` · `source` · `destination` · `complement` · `topic` · `measure`
*Circumstances:* `location` · `time` · `manner` · `duration` · `path` · `comitative`

*Added 2026-09-11 at E2 task 2 — named rows, operators and scope. Evidence: `202609111511_notes.md`.*

33. **A zip is a FLAT LIST of rows; nesting is naming** — the Tseitin convention. Arbitrary depth, constant shape. *OQ8 answered.*
34. **A box may hold a ROW NAME**, and its vector is that row's **derived point** (reqs 12, 16) — so embedded thoughts compare as thoughts, while the rows survive for the binding machinery.
35. **ROW ORDER = SCOPE ORDER**, and the prefix carries **five** scope-bearing elements: `quantifier · negation · modality · attitude · domain`. That set is FRAME and must stay exhaustive. Row order is therefore **not free** — it can never be used to encode anything else. *OQ6 answered.*
36. **Quantification uses BINDER ROWS** — a box holds a variable, a row binds it. One binding mechanism for quantification, questions, equations and naming (req 2 · evaluator req 5), at the cost of an indirection on every quantified comparison.
37. **There is no CAUSE relation** — a cause is `IMPLY` read with the theatre's arrow of time. Premise, stated so it is not assumed silently: *in a deterministic world, a cause is what implies its effect.* A bonus falls out: when implication direction and time direction **disagree**, the «because» is evidential, not causal.
38. **Assertion status distinguishes «because» from «if»** — same `IMPLY`, rows asserted or not. Binding state (req 2) therefore carries truth-conditional weight and is not bookkeeping.
39. **«only» converts `IMPLY` into `EQ`**, uniformly across the family (*if/iff · because/only because · causes/only causes*). It is a marker, not an operator; the marker words are `language_closed_classes` rows. *Same seam as the role markers.*
40. **Purpose is POV(want) + `IMPLY`** — the intended outcome sits inside the attitude and is never asserted. *OQ3 answered; nothing survives as a purpose box or a purpose relation.*
41. **The operator set is the TEN non-degenerate binary truth functions** — `AND · NAND · OR · NOR · XOR · EQ · IMPLY · NIMPLY · CONV · NCONV` — frame, closed by mathematics, **never trimmed to what English marks**. Trimming would restrict what tokeniko can THINK to what English can SAY. The six English marks are KB rows.
42. **`CONV` is not `IMPLY` with the rows swapped** — row order carries scope (req 35), so it cannot also carry direction.
43. **Truth is cheap for all ten; COMPOSITION is not** — folding two derived points has a natural reading for `AND`/`OR`/`NOT` and none obvious for `XOR`/`NAND`. Declare all ten, implement all ten in truth, and let the derived layer **abstain visibly** where it cannot define the composition.
44. **Normalization is a comparison-time step, not a trim** — `A NIMPLY B` and `A AND ¬B` compare as one thought and still render as «regardless» and «and not».
45. **The attitude takes a prefix position** — de re / de dicto must be expressible. A flat-only POV does not abstain, it **silently forces de re** and would assert existence the speaker never claimed («he thinks a unicorn is in the garden»). The flat POV field survives as shorthand for one fixed prefix position.
46. **`domain` is the fifth prefix element** — «legally» · «in chess» · «as a doctor» · «in Italy» (a jurisdiction, provably not the `location` box: «in Italy you may drive in France…»). No type column — nothing reasons differently across kinds. **Requirement 6 needs no new machinery**, and «as a doctor I disagree; as a father I understand» stops being a KB contradiction.

*Added 2026-09-11 at E2 task 3 — binding states and moods. Evidence: `202609111607_notes.md`.*

47. **Binding state is a property of ANY SLOT** — a role box, a field inside a nominal record, and **the row's TRUTH**. Three values: `BOUND` · `OPEN` · `EMPTY`. Unbound ≠ 0 (req 2) applied uniformly.
48. **MOOD IS NOT A FIELD** — it is read off what is open. Question = something OPEN (a box for wh-questions, the truth slot for polar ones) · imperative = POV(want) over an unasserted row · supposition = POV(suppose) · optative = POV(wish) · forecast = a future theatre. Nothing was found that mood would have to store.
49. **A question and an equation are the SAME ZIP** — anything open is an open thought, and whether he ASKS it or SOLVES it is a choice made later, never a shape. *(The Captain's own 2026-08-11 ruling: «the unbound variable IS the urge, and memory-vs-ask is only which source might bind it».)*
50. **An OPEN slot may carry a PRIOR** — a [0,1] expectation of how it will bind. «It's cold, isn't it?» is open with a high prior; «Is it cold?» is open with none. This is the one genuinely new scalar the bench found.
51. **Scalar wherever there is a QUANTITY** — a scalar contains the discrete case, so nothing is foreclosed. But a scalar on a NON-quantity manufactures a threshold, and a threshold is policy (a measured db row with a note). v1's `dubitative = 0.5 neutral / 1.0 question` is the tell: the midpoint means «none», not «half».
52. **The nominal record's sixth field is `sense`, independently bindable** — the parser emits the lemma with the sense slot OPEN (brain req 7); a box is BOUND as to its word and OPEN as to its meaning, in one place rather than v1's parallel map.
53. **`INAPPLICABLE` is not a stored state** — whether `sleep` licenses a patient is a LEXICAL fact answered by the KB. Storing it would duplicate knowledge and go stale («sleep the sleep of the just»). An unfilled box is `EMPTY`; the lexicon says whether that is expected.
54. **Irony is not a schema field** — it is a rhetorical figure and belongs to the figurative layer (reqs 13–14): micro-nn proposes the reading, the evaluator admits it. v1's `ironic` scalar is v1 doing pragmatics inside the format for want of anywhere else.
55. **Attitude classes are an ANCHOR SET, never an enum** — attitude verbs are open (think · believe · suppose · want · fear · pretend · hope · doubt…), so nearest-anchor geometry with a small anchor set, not v1's four-valued `klass`. The heart needs «supposed or believed?» reliably and an anchor set never misses.
56. **Assertion status is FIRST-CLASS AND VISIBLE, not an internal detail of the evaluator's search** — the heart reads it too, to fire suppositions at the imagination gain (heart 16). It is also what separates «because» from «if» (req 38).
57. **Three quantities, two slots — never conflated**: `truth` (is it so? fuzzy) and `forecast confidence` (will it be so?) share the row's truth slot, disambiguated by whether the theatre is past or future (heart 17); `parse confidence` («did I understand this sentence?», parser-compiler req 4) is a property of the WHOLE ZIP and separate. A zip can be perfectly understood and probably false.

*Added 2026-09-12 at E2 task 4 — confidence and provenance. Evidence: `202609121556_notes.md`.*

58. **`parse_confidence` is ONE scalar on the whole zip, and may be EMPTY** — «this zip IS what I received» (parser-compiler req 4). One number because its calibration signal («did the speaker correct me?») arrives per UTTERANCE: a per-row scalar would have no training signal and could never come to mean anything. Per-part doubt is already carried by binding state + prior (reqs 47, 50) — a half-heard word is `OPEN`, never `BOUND` to a guess with a low number. EMPTY when no parse happened (self-talk, chaining): 1.0 would claim perfect understanding of an utterance that never existed.
59. **Provenance, `derived_by` and `original` are DOCUMENT-level, not zip fields** — all three were built at E0. POV(say) is *content* («Anna said it»); provenance is *where this belief reached me* (Bob told me). Both can be present in one zip and neither reduces to the other.
60. **A resolved forecast mints a NEW belief; the stored zip is never mutated** — the forecast's `status` and `resolved_at` move (E0), the heart spikes, and «I expected rain at 0.7» stays true forever. *Retreat, not override, applied to the future half of the timeline.* **Consequence for E4:** stake resolution must compare content while IGNORING the theatre's tense.

*Added 2026-09-12 at E2 task 5 — storage (OQ9). Evidence: `202609121807_notes.md`.*

61. **Storage is SPARSE; fixed arity is a property of the SCHEMA, not of the storage** — only non-EMPTY slots are stored. Measured on PropBank's 112,917 predicate instances: **mean 2.59 roles per row of 18 slots — ~80% of every row is empty**, and 90.7% of sentences are four rows or fewer. *OQ9 answered.*
62. **The KEY is the truth; the VECTOR is an epoch-stamped CACHE** (dictionary req 13). A base rebuild therefore invalidates only the cache — the sleep phase re-derives lazily (req 16's pattern), and even eager re-derivation is ~9 dictionary lookups per sentence, i.e. **seconds for a whole biography**. **Consequence: E9's translation night is for a SCHEMA change; a dictionary rebuild never touches the biography.**
63. **The THEATRE is a cache too** — truth is the filler plus the utterance time. Recomputing «yesterday» years later needs both that timestamp and the station's version-dependent resolution logic, so the theatre is stored and epoch-stamped like the role vectors.
64. **Row names are ZIP-LOCAL** — Tseitin names never cross thoughts; a cross-thought reference is a document id. So a zip is self-contained and *can* be embedded in its document. *(Whether it is, is data-modeling's call, not tkzip's.)*
65. **The nominal record's seventh field is `marker`** — the preposition actually used. «I walk **to** the station» and «I walk **toward** the station» fill the same box and do not mean the same thing; without the marker the difference is lost. v1 carries markers for exactly this («Japan is IN Asia» vs «Japan IS Asia») and the marker words are already `language_closed_classes` rows, so this stores WHICH ROW WAS MATCHED, not new knowledge.
66. **The derived point's density is NOT decided by this chapter** — it follows from the composition operator, and composition is not defined for all ten (req 43); a rotation densifies. E0's dense `list[float]` stands as the safe default rather than being narrowed on a guess.

*Added 2026-09-14 at E2 task 6 — the drill and the freeze. Evidence: `202609141009_notes.md`; the bar,
declared first: `202609140917_the-drill-bar.md` (commit `1a68337`).*

67. **`direction` is the eighteenth role** — «He looked **up**», «She turned **left**»: a direction with NO ENDPOINT, which is not a `destination`, not a `path` and not a `manner`. **FORCED BY THE DRILL** — the first instance of the Captain's task-1 prediction that a missing slot would be found by an instrument and fixed by redesigning the frame, never by a migration.
68. **A prefix element NAMES THE MATRIX it scopes (`scopes`), and never another prefix row** — forced by «software **can** be minds and humans **must** be minds»: `◇A ∧ □B` cannot be said by elements that scope «everything after». Because a named row may be a JOIN covering a subtree, this gives arbitrary nesting for free. The matrix-only restriction is what keeps req 35 true: nesting among prefix elements is their ORDER, and two spellings of `¬∀` are refused.
69. **A box may hold a ROW NAME as a typed `Ref`** — «the result **of** perception and learning and reasoning». Req 34 said this was allowed and no type existed for it, so the name validated as a bare dictionary key. The mildest forced change: an omission against a requirement already made.
70. **Attributive adjectives are SECOND ROWS, not a field** — «a human body» is ∃B(body(B) ∧ human(B)), the same machinery as the depictive «he ate the fish raw». `degree` is for «*very* big», an intensifier on a property, and conflating the two was a QM error the drill's self-audit caught. Costs row inflation; sparse storage absorbs it at ~250 bytes a row. **[BUILT 2026-09-16 (E3) — and building it added one thing the requirement does not say: an attributive adjective FORCES a binder even where no quantifier word appears, because a row reading `patient=body.n, complement=human.a` claims that BODIES are human. There is no way to say «this particular body, which is human» without a variable, so the adjective raises the one req 36 already provides — existential, with the phrase's own determination beside it. Record: `../parser-compiler/202609161215_the-attributive-adjective.md`.]**
71. **Single-row negation is `truth = 0.0`** — claimed false. `NegationRow` is reserved for negation that must scope OVER another prefix element (`¬∀` versus `∀¬`), which is the only case where the order matters.
72. **AMENDS REQ 23 — it over-grouped three words.** `never` is negation plus a universal over the time variable (collapses as ruled) · `hardly` is a manner box carrying only a degree · **`almost` is neither**: «he almost died» asserts he did NOT die AND that the event was close, and closeness-to-an-event-that-did-not-happen has no home. It is the drill's only abstention, honestly marked, on two witnesses.
73. **THE SCHEMA IS FROZEN AT v2.0** — `SCHEMA_VERSION = 2`. Every change from here is a migration under the Captain's hand, never an edit. That is what the version integer on every zip (req 22) exists for, and what E9's translation night translates.
